// DEPRECATED: Legacy Firebase enums - replaced by Supabase implementation
// This file provides compatibility for legacy code during migration

// Placeholder enum for legacy compatibility
enum TipoVeiculoEnum {
  carro,
  moto,
  bicicleta,
  caminhao,
}

// Placeholder enum for legacy compatibility  
enum StatusViagemEnum {
  solicitada,
  aceita,
  iniciada,
  finalizada,
  cancelada,
}

// Placeholder enum for legacy compatibility
enum TipoPagamentoEnum {
  dinheiro,
  cartao,
  pix,
}

// Add other legacy enums as needed during migration